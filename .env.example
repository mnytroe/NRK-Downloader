# --------- Runtime mode ---------

# 'production' i prod, 'development' lokalt
NODE_ENV=production

# --------- Server ---------

# Port for Next.js (kan stå tom – Next bruker 3000)
PORT=3000

# --------- Sikkerhet / domenefilter ---------

# Kommaseparert liste over tillatte vertsnavn (uten protokoll)
ALLOW_DOMAINS=nrk.no,tv.nrk.no,www.nrk.no,radio.nrk.no,nrkbeta.no

# --------- Rate limiting ---------

# Maks kall per IP per minutt (globalt)
RATE_LIMIT_PER_MINUTE=30

# (valgfritt) Redis-URL for prod, f.eks. redis://default:pass@redis:6379/0
# REDIS_URL=

# --------- Midlertidige filer ---------

# Mappe for midlertidige nedlastinger (fallback: OS tmpdir)
# TMP_DIR=/tmp/nrk-downloader

# --------- Logging ---------

# 'debug' | 'info' | 'warn' | 'error'  (fallback basert på NODE_ENV)
# LOG_LEVEL=info